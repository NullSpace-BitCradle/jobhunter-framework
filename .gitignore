# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.env
.env.*

# User-local config (examples remain tracked)
/config.yaml
/config.local.yaml
/discovery/config/companies.yaml
/discovery/config/keywords.yaml
/discovery/config/filters.yaml

# Discovery runtime data
/discovery/state/
/discovery/output/

# Personal career content - never commit
Master_Career_Document*.md
Job_Description-*.md
applications.md
/output/

# Session-specific PRDs and review docs (per-application work dirs)
MEMORY/
*.pdf
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Keep the shipped LaTeX templates tracked
!templates/*.tex

# OS
.DS_Store
Thumbs.db

# Claude Code sandbox-injected files. The sandbox auto-creates several 0-byte
# or character-device files at runtime that should never be tracked:
#   - .claude/settings.json, settings.local.json, skills (character devices)
#   - .gitmodules (ext4 mount point, not a real submodule file)
#   - Node/Bun/npm scaffolding (empty package.json, lockfiles, rc files) that
#     gets re-dropped into project dirs even after deletion
#   - Shell rc files, IDE dirs, MCP config, and misc dotfiles the sandbox drops
#     as read-only 0-byte decoys to prevent tool escape to the user's real home
**/settings.json
**/settings.local.json
.claude/skills
discovery/.claude/
/.gitmodules
/.bash_profile
/.bashrc
/.gitconfig
/.github
/.idea
/.mcp.json
/.profile
/.ripgreprc
/.vscode
/.zprofile
/.zshrc
/scripts
/discovery/.gitmodules
/discovery/.npmrc
/discovery/.yarnrc
/discovery/.yarnrc.yml
/discovery/bunfig.toml
/discovery/package.json
/discovery/package-lock.json
/discovery/pnpm-lock.yaml
/discovery/yarn.lock
